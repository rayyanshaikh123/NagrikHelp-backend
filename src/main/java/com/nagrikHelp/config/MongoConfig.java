package com.nagrikHelp.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MongoConfig {
    // Using Spring Boot auto-configuration for MongoDB via application.properties.
    // Customize MongoTemplate or converters here if needed in future phases.
}
