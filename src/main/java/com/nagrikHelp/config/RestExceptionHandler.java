package com.nagrikHelp.config;

// This class was originally added to provide additional error formatting during development.
// A controller-level RestExceptionHandler already exists in com.nagrikHelp.controller.RestExceptionHandler.
// To avoid a bean name conflict we intentionally keep this class as a plain utility holder (no
// Spring annotations). If you want to contribute shared formatting helpers, add static methods here.

class ConfigRestExceptionHandler {
    // Intentionally left blank to avoid duplicate @ControllerAdvice beans. Kept for reference.
}
